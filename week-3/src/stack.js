export function balancedBrackets(str) {
    const stack = [];
    const openingBrackets = '({[';
    const closingBrackets = ')}]';
    const bracketPairs = {
        '(': ')',
        '{': '}',
        '[': ']'
    };
}